package Preprocessing

import java.util.Random

import com.github.vickumar1981.stringdistance.StringConverter._



case class Preprocessor(){

  private def tresholdRandom(n: Int, t: Int, Data: List[Map[String, String]]): Unit = {

    val processedData = Data.filter()

    val rand = new Random(System.currentTimeMillis())
    val random_index = rand.nextInt(Data.length)


  }

  private def tresholdEqual(n: Int, t: Int, Data: List[Map[String, String]]): Unit = {

  }

  def preprocessing(): Unit ={

  }
}
